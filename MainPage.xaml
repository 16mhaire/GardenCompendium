<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GardenCompendium.MainPage">


    <AbsoluteLayout
        x:Name="GardenLayout"
        BackgroundColor="LightGreen">
    </AbsoluteLayout>
</ContentPage>
<!--   <ScrollView>
        <VerticalStackLayout
            Padding="30, 0"
            Spacing="25">

            <Image
                Source="garden_compendium_logo.png"
                HeightRequest="185"
                Aspect="AspectFit"
                SemanticProperties.Description="Garden Compendium Logo WIP"/>

            <Label
                Text="{Binding ZipCode}"
                HorizontalOptions="Center"
                VerticalOptions="Center"/>

            <Button
                Text="Open Menu"
                Clicked="OnOpenFlyoutMenuClicked"/>

            <CollectionView x:Name="MyPlantsCollectionView"
                            ItemsSource="{Binding Plants}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <VerticalStackLayout Padding="10">
                            <Label Text="Binding Name"
                                   FontAttributes="Bold"
                                   FontSize="Medium"/>
                            <Label Text="{Binding ScientificName[0]}"
                                   FontSize="Small"/>
                        </VerticalStackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>-->